---
layout: layout.njk
---

<section class="block" aria-labelledby="blog-title">
  <h1 class="t-xl mb-05">
    Blog
  </h1>
  <p class="tc-light mt-05">Todos los artículos de este blog, tratando desde Ingeniería de Software a música.</p>
  <ul class="gap-m clean">
    {% for post in collections.post | byLocale("es") | reverse %}
      <li>
        <article>
          <a href="{{ post.url }}">
            <h2 class="din t-l mb-05">{{ post.data.title }}</h2>
          </a>
          <p class="mt-0 mw-text">
            {{ post.data.description }}.
          </p>
          <p class="tc-light t-s">
            <date class="mr-1">
              {{ post.data.date | date("MMMM DD, YYYY") }}
            </date>
            <span class="tags">
              {% for tag in post.data.tags | filterList("post") %}
                <a href="/tags/{{tag}}" class="tag">#{{ tag }}</a>
              {% endfor %}
            </span>
          </p>
        </article>
      </li>
    {% endfor %}
  </ul>
</section>